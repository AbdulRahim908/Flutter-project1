import 'package:flutter/material.dart';
customludowidget(colors) {
  return Container(
      height: 150,
      // color: colors,
      width: 150,
      padding: const EdgeInsets.all(16.0),
      decoration: BoxDecoration(border: Border.all(),color: colors)      );
}

customLudowidget(colors) {
  return Container(
      height: 25,
      // color: colors,
      width: 25,
      decoration: BoxDecoration(border: Border.all(),color: colors)
      
      );

}
customHomewidget(colors){
  return Container(
      height: 40,
      // color: colors,
      width: 40,
      decoration: BoxDecoration(border: Border.all(),color: colors)
      );
}